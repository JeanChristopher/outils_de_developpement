if(ajax.readyState == 4 && ajax.status == 200) {
			var jsonObj = JSON.parse(ajax.responseText);
			var features = jsonObj.features;
			for (var i = 0; i < features.length; i++ ){
				var JsonLongitude = jsonObj.features[i].geometry.coordinates[0];
				var JsonLatitude = jsonObj.features[i].geometry.coordinates[1];
				L.marker([JsonLatitude,JsonLongitude]).addTo(layerMarkers);
			}
			map.fitBounds(layerMarkers.getBounds());
		}
